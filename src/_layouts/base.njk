<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "head.njk" %}
    {% if site.env %}
      <!-- capture the CSS content as a Liquid variable -->
      {% set css %}{% include "app.compiled.css" %}{% endset %}
      <style>
        {{css | safe}}
      </style>
    {% else %}
      <link rel="stylesheet" href="/static/app.compiled.css">
    {% endif %}

  </head>
  <body class="antialiased relative"
          style='background-image: url("./static/img/ps1.jpg");'>
    <div class="text-gray-300 min-h-screen flex items-center bg-black bg-opacity-75">
      <div class="container mx-auto p-4 flex flex-wrap items-center">
        <div class="w-full md:w-5/12 text-center p-4">
          <img src="../../static/img/vintage_tv.svg" alt="Vintage TV"/>
        </div>
        <div class="w-full md:w-7/12 text-center md:text-left p-4">
          <div class="text-l font-medium text-green-500">Website Under Construction</div>
          <div class="text-6xl font-medium text-yellow-500">Franklin Riley</div>
          <div class="text-xl md:text-2xl font-medium mb-4 text-justify text-ltblue-500">
          I am a Software Engineer, and I also run a Web Development Company named MODIBLE.  Let me know if you need any assistance with your website.
      </div>
          <div class="text-4xl mb-8 text-green-500">
            <a href="mailto:modible@gmail.com">Modible@Gmail.com</a>
          </div>
        </div>
      </body>
      <script src="/static/app.bundled.js"></script>
      <script>
        function toggleNavbar(collapseID) {
          document
            .getElementById(collapseID)
            .classList
            .toggle("hidden");
          document
            .getElementById(collapseID)
            .classList
            .toggle("block");
        }
      </script>
    </html>